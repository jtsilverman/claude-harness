# Slow-loop ledger

Empty template. The grader (slow loop) appends one line per shipped spec here: a terse
record of what the grade found and what side effect it routed (memory candidate, doc
fault, reader-model update). It is an audit trail, not config.

It ships empty and grows one line at a time as you ship specs.
